
# ========= Scraper =========
google-api-python-client>=2.0.0
yt-dlp>=2024.0.0
youtube-transcript-api
requests

# ========= Embedding / Vector DB =========
sentence-transformers>=2.2.0
transformers<5
faiss-cpu>=1.7.0
numpy>=1.24.0
rank-bm25>=0.2.2
tqdm>=4.66.0

# ========= Dataset export =========
pandas>=2.0.0
pyarrow>=12.0.0
# thu vien

langchain-community
langchain-core
jq
langchain_huggingface
langchain-ollama

# ========= LLM judge providers (for evaluate_ragas.py) =========
# Install whichever you have a key for. .env file loading is optional.
google-genai           # Gemini (deprecated, ~20 RPD free)
groq                   # Groq Cloud (~14k RPD on 8B)
python-dotenv          # .env file loading
# cerebras-cloud-sdk   # optional — only if not using OpenAI-compat HTTP
# mistralai            # optional — only if not using OpenAI-compat HTTP

# ========= Answer-step evaluation metrics (evaluation/answer_eval.py) =========
nltk                   # for BLEU score
rouge                  # for ROUGE-1/2/L
